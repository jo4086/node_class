// 주어진 문자열을 대문자로 변환하는 함수 toUpperCase()를 내보내는 코드를 작성하세요.

function toUpperCase(data) {
    return data.toUpperCase()
}
export default toUpperCase

export function toUpperCase2(data) {
    return data.toUpperCase()
}
